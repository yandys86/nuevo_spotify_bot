# Copia a `.env` y rellena. `.env` está gitignored.
# Token del bot @YiyoLMB_Monitor_bot creado en @BotFather.
TELEGRAM_TOKEN=8964808146:AAE95QE4321DrEi2jrSTZ6y4KjlG3jtB-UE

# IDs de chat destino separados por coma. Puedes obtenerlos:
#   1. En Telegram, envía /start al bot @YiyoLMB_Monitor_bot desde cada
#      teléfono que quiere recibir.
#   2. Luego corre en el host:
#        curl -s "https://api.telegram.org/bot$TELEGRAM_TOKEN/getUpdates" | \
#          python3 -c "import json,sys;d=json.load(sys.stdin);print(set(u['message']['chat']['id'] for u in d['result'] if 'message' in u))"
TELEGRAM_CHAT_IDS=699683569,828562504
